import "package:flutter/material.dart";

class ChannilLogo extends StatelessWidget {
  const ChannilLogo();

  @override
  Widget build(BuildContext context) => Image.asset("assets/logo2.png", scale: 3);
}
