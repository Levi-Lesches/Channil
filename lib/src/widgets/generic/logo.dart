import "package:flutter/material.dart";

class ChannilLogo extends StatelessWidget {
  const ChannilLogo();

  @override
  Widget build(BuildContext context) => SizedBox(
    // height: 80, 
    // width: 250, 
    child: Image.asset("assets/logo.png", scale: 5),
  );
}
